jdk: openjdk17

before_install:
  - chmod +x gradlew
  - ./gradlew clean

install:
  - ./gradlew :androidlame:assembleRelease
  - ./gradlew :androidlame:generatePublishConfig

build_commands:
  - ./gradlew :androidlame:publishToMavenLocal
